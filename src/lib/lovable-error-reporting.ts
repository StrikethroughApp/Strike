// Deprecated compatibility shim: avoid mentioning the previous vendor/branding
// Re-export the neutral API from the local non-branded module.

export { reportAppError as reportLovableError } from "./error-reporting";
